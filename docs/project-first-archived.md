# Archived: project-first

`project-first` was removed as an active skill.

Reason:

- it was game-branded,
- it depended on commands that are no longer in this repository,
- and its routing role is now covered by the root `AGENTS.md`.

If a lightweight onboarding skill is needed later, rebuild it from the current repo structure instead of restoring the donor version.
